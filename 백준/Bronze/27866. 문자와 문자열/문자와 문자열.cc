#include <bits/stdc++.h>

int main() {

	std::ios::sync_with_stdio(0);
	std::cin.tie(0);
	std::cout.tie(0);

	std::string S;\
	int i;
	std::cin >> S >> i;
	std::cout << S.at(i-1);

	return 0;
}
